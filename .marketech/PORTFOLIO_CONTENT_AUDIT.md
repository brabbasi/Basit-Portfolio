# Portfolio public-truth audit — 2026-09-20

This draft is a **non-production truth/integrity repair**, not a résumé rewrite.

## Corrected in this draft

- removed links to `project7.html` and `project8.html`, which do not exist in the repository;
- removed references to missing skill/certification image assets;
- removed the certification section until each credential can be evidence-verified;
- removed unattributed generic testimonials until provenance/permission exists;
- removed the inert contact form that had no submission handler;
- removed placeholder Open Graph URL/image metadata;
- retained intentionally public social links while adding `noopener noreferrer` protection;
- added mobile timeline/contact hardening, focus visibility and reduced-motion behavior;
- advanced the footer year to 2026;
- added `scripts/verify-portfolio-integrity.mjs` to fail on missing local assets/links and the known trust regressions above.

## Deliberately not rewritten yet

Existing employment, education and project-page narrative is left unchanged in this pass. Those claims should be reconciled against the current résumé/source evidence before a production portfolio refresh.

No production deployment is authorized by this draft.
